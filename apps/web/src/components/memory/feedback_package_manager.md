---
name: package_manager
description: Use bun as the package manager, not pnpm or npm
type: feedback
---

Always use `bun` as the package manager for this project.

**Why:** User corrected pnpm usage and specified bun.

**How to apply:** Use `bun add`, `bun install`, etc. for all package management commands.
